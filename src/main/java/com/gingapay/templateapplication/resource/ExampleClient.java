package com.gingapay.templateapplication.resource;

/*
    A feign client example:

        @FeignClient(name = "gingapay-example-client", url = "${gingapay.example.webservice-uri}", configuration = FeignClientConfiguration.class)
        public interface ExampleClient {

            @GetMapping(path = "/api/v1/example", produces = MediaType.APPLICATION_JSON_VALUE, consumes = MediaType.APPLICATION_FORM_URLENCODED_VALUE)
            ExampleResponse getExample(@RequestHeader("Authorization") String authorizationHeader);

        }

* */


